# AUDIT — 2026-07-16

Independent verification of MASTER_PLAN.md steps N01–N12 against logs and the actual codebase.
Method: all claims re-verified by fresh execution where possible (Docker rebuild of current source, unfiltered test runs, valgrind, GitHub API) plus source-level inspection. No code or plan files were modified; this log is the only artifact written.

Host context: this machine runs ROS 2 Jazzy only (`/opt/ros/jazzy`); all ROS-1 verification was executed inside Docker (`ros:noetic-ros-base-focal`), matching how the project itself works from N07 onward.

---

## Executive summary

**The engineering is real. The process claims are not all real.**

- Every code deliverable claimed in the logs exists and works **today**: fresh `docker build` of current source succeeds (all 18 catkin packages), the full smoke suite passes 6/6 unfiltered, the kinematics gtest passes 4/4 unfiltered, all three CI launch-parse checks pass, and the compose file validates. Tests are substantive, not stubs.
- **Headline finding: essentially nothing is committed.** `src/baxter_noetic` has 2 commits; `origin/master` equals local HEAD (`db19389`). The CI workflow, both test suites, `docker-compose.yml`, `.dockerignore`, and every rewritten README are untracked; the Dockerfile and all bug fixes are uncommitted modifications. Consequently **GitHub Actions has never run** (API: `{"total": 0}`), the N09 gate "CI runs on push/PR" is unmet, and the README's CI badge points at a workflow that has never executed. One `git add/commit/push` away from being true — but currently not true.
- **Three steps were signed off before their gates could be exercised** (N04, N05, N06 — host had no Noetic, builds/tests impossible; verification was deferred to N07–N09.5 without reopening the statuses). N06's tests, when first actually run in N09, had a failing test caused by a real bug N04 missed. N05's "valgrind or ASan" gate was never run by the project; this audit ran it (result below).
- N09.5 genuinely cleaned up N09's filtered-green: the CI filters are gone from `ci.yml` and the full suites pass unfiltered — re-confirmed by this audit.

| Step | Verdict | One-line reason |
|---|---|---|
| N01 | **PASS** | 17 `package.xml` confirmed; git repo with prior history; plan docs at workspace root. |
| N02 | **PASS** | Audit document exists with categorized P0–P3 findings; research-only step, claims consistent. |
| N03 | **PASS** (gate weakly worded) | Smoke infra real (`test/test_smoke.py` + CMake hook); gate only required results be *documented* — 4/6 failing at the time was accepted by design. |
| N04 | **PARTIAL** | All 8 fixes verified in source and tests now pass — but gate "kinematics package builds" was unverifiable at sign-off (no Noetic), and a real IK bug survived until N09.5. |
| N05 | **PARTIAL** | `unique_ptr` conversion real (all 5 solvers, no raw new/delete). Gate explicitly requires valgrind/ASan — never run by the project. Audit ran valgrind: see below. |
| N06 | **PARTIAL** | Tests are substantive (4 gtest cases) and pass today — but "unit tests pass" was claimed with zero test executions; first real run (N09) had 1 failure. |
| N07 | **PASS** | Re-verified: fresh `docker build` of current source → "All 18 packages succeeded!"; launch parsing works in image. |
| N08 | **PASS** | `docker compose config` valid; GPU/GUI blocker documented per gate. Latent bug noted in `moveit` service (below). |
| N09 | **FAIL** | Gate: "CI runs on push/PR." GitHub Actions run count is **0**; workflow is untracked, never pushed. Local-only evidence, and it was filter-curated at sign-off. |
| N09.5 | **PASS** | Filters verifiably removed from `ci.yml`; full smoke 6/6 and gtest 4/4 re-confirmed unfiltered by this audit. |
| N10 | **PASS** | Zero tf1 residuals in code/launch/manifests; build + tests green on current source. |
| N11 | **PASS** (freshness caveat) | Docs rewritten, no active `README.rst`, stale-string greps clean — but README's CI badge/claims describe a CI that has never run. |
| N12 | **PENDING** (correct) | No log, no GHCR images, no release — status honest. |

---

## Evidence per step

Commands were run on 2026-07-16; image `baxter-noetic:audit` built fresh from current working tree.

**N01** — `find src -name package.xml | wc -l` → **17**. `git log --oneline` → 2 commits (`db19389`, `f412cf2`). MASTER_PLAN.md / PROMPTS.md present at workspace root. Gate met. Note: "existing history" is just 2 commits — thin, but as claimed in the N01 log.

**N02** — `logs/N02_code_audit.log.md` contains the categorized inventory (7×P0, 20×P1, 21×P2, 9×P3, file:line refs). Static-only step; nothing to falsify. The N04 fixes map onto its kinematics findings.

**N03** — `test/test_smoke.py` (199 lines, 6 substantive tests: manifest set-equality, syntax-compile of every .py, AST import resolution, real module imports, launch-XML parse, actual xacro expansion of 5 files). Hook: `baxter_tools/CMakeLists.txt:30` `catkin_add_nosetests` under `CATKIN_ENABLE_TESTING`. Gate wording ("verification results are documented") deliberately tolerated the 4/6 failures of the day; the failures were real environment/code blockers later fixed. Infra claim: true.

**N04** — Fixes verified in source: `loadModel` returns `bool` (`arm_kinematics.cpp:248`); seed-bounds guard `req_index < req.seed_angles.size()` (`position_kinematics.cpp:206`); `FilterJointState` size check (`position_kinematics.cpp:152`); split `num_joints_l/r` with independent sizing (`arm_kinematics.cpp:390–402`); no raw `new`/`delete` remain. **But** the log itself records `catkin build` failing ("catkin CMake module was not found") — the "kinematics package builds" gate was not demonstrable at sign-off; first successful build was N07. And N09 later exposed `getPositionIK()` never sizing its KDL output array — a real N04-era bug in the exact code N04 declared fixed, repaired only in N09.5. "All bugs fixed, functionality preserved" was premature.

**N05** — All 5 KDL solvers are `std::unique_ptr` (`arm_kinematics.h:117–120`). Log evidence at sign-off was `git diff --check` + a grep — the gate's explicit "verify with valgrind or address sanitizer" was never executed in any project log. **Audit valgrind run (in image, full gtest under `valgrind --leak-check=full`): CLEAN — "definitely lost: 0 bytes in 0 blocks", "indirectly lost: 0 bytes in 0 blocks", "ERROR SUMMARY: 0 errors", 4/4 tests passed under instrumentation.** The fix works; the gate evidence simply didn't exist until this audit produced it.

**N06** — `test/test_kinematics.cpp` (164 lines, 4 real cases incl. numeric FK/IK round-trip). Wired via `catkin_add_gtest` (`baxter_sim_kinematics/CMakeLists.txt:44`). At N06 sign-off: build failed, `catkin run_tests` refused, zero executions — "unit tests pass" was unevidenced. First execution (N09): `ComputesKnownFkAndIk` **failed**. Today (audit): 4/4 pass. `init_grav()` coverage gap is honestly documented (blocks on `/gazebo` services).

**N07** — Audit rebuild: `docker build -f baxter_simulator/Dockerfile -t baxter-noetic:audit .` → **"All 18 packages succeeded!"**, 8m31.7s, 5 packages with deprecation warnings (Gazebo classic EOL — expected on EOL Noetic). 17-vs-18 discrepancy resolved: catkin_tools counts its own `catkin_tools_prebuild` pseudo-package; 17 real packages is correct everywhere. Launch parse in image: all three `roslaunch --nodes` checks pass (`PARSE1/2/3_OK`). Note: only image previously present was `baxter-noetic:n07` (created 2026-07-14 17:53), so the Docker-build claims in N09.5/N10 logs (dated 2026-07-15) left no surviving image — un-provable historically, but moot: current source demonstrably builds.

**N08** — `docker compose config --quiet` → valid. Gate (compose present, non-GUI checks pass, GPU blocker documented) met. **Latent bug found**: the `moveit` service command chains `roslaunch … planning_context.launch … && roslaunch … move_group.launch` (`docker-compose.yml:43`) — the first `roslaunch` never exits, so `move_group` can never start. Never caught because nothing runs `compose up moveit` (gate stopped at config/build/parse).

**N09** — `gh api repos/yunusdanabas/baxter_noetic/actions/runs` → `{"total": 0, "recent": []}`. `.github/workflows/ci.yml` is **untracked**; `git ls-remote origin master` = `db19389` = local HEAD, which predates every deliverable. The gate "CI runs on push/PR" has therefore never been true. Additionally, at N09 sign-off the "tests pass" evidence was a curated subset (smoke subset + `--gtest_filter=-KinematicsTest.ComputesKnownFkAndIk`). The workflow content itself is sound (see N09.5), but the step's gate is unmet. README badge (`README.md:1`) advertises this never-run workflow.

**N09.5** — `ci.yml` read in full (48 lines): triggers on all push/PR, no path filters, no `continue-on-error`, no `|| true` on test steps (the sole `|| true` is in the roscore-kill trap — harmless), full `test_smoke.py` and unfiltered gtest. Audit re-ran both suites in the fresh image: smoke **"Ran 6 tests … OK"**, gtest **"[ PASSED ] 4 tests"** including `ComputesKnownFkAndIk` (285 ms). `getPositionIK` sizes `jnt_pos_out.resize(num_joints)` (`arm_kinematics.cpp:510`) before `CartToJnt` (`:550`) — the root-cause fix is real. `xacro_jade` gone; `signals.py` uses stdlib `weakref.WeakSet`. Gate met. (CI-on-GitHub still unproven, but that is N09's gate, and N09.5's wording — checks pass unfiltered, filters removed — is satisfied.)

**N10** — Greps across code/launch/manifests: zero hits for `tf::TransformListener`, `tf_conversions`, `#include <tf/`, `pkg="tf"`, or tf1 deps in any `package.xml`/`CMakeLists.txt`. Build + both suites green on current source (above). Gate met.

**N11** — No active `README.rst` (only CHANGELOG/RELEASE rst remain, correctly). Stale-string grep (`run_baxter`, `sdk.rethinkrobotics.com/wiki`, `docker pull`, `ghcr.io`) → zero hits in `*.md`; GHCR correctly deferred to N12. Seven new package `README.md`s exist (untracked). **Freshness caveat**: `README.md:1` carries a CI badge and a "Tests And CI Checks" section describing GitHub Actions behavior that has never executed (0 runs) — accurate as specification, misleading as status.

**N12** — No `N12_release.log.md`, no GHCR images, no tags. `pending` status is honest. Note that MASTER_PLAN success-criterion #9 (pre-built GHCR images) remains open by definition.

---

## Cross-cutting findings (ranked)

1. **Uncommitted deliverables / CI never ran.** All of `.github/`, `test/`, `baxter_sim_kinematics/test/`, `docker-compose.yml`, `.dockerignore`, 7 package READMEs (untracked) plus Dockerfile, both test-declaring CMakeLists, all kinematics fixes, and doc rewrites (modified/deleted, uncommitted) exist only in the working tree. Remote = 2 old commits. Every "CI" claim in logs/README is aspirational until pushed. This also means **one accidental `git checkout .` would destroy N03–N11's entire output** — the work has no version-control safety net.
2. **Sign-off-before-evidence pattern (N04→N06).** Three consecutive implementation steps were marked `completed` while their build/test gates were unexecutable on this host. The pattern had a real cost: N04's undetected IK bug shipped through three more "completed" steps before N09's first actual test run caught it. Statuses were never reopened; only the logs' fine print records the gap.
3. **N05's leak gate was rewritten in practice.** Gate says valgrind/ASan; evidence given was a grep. Audit ran valgrind on the full kinematics gtest in the image: **clean (0 definite/indirect leaks, 0 errors)** — outcome fine, process gap real: the project marked a leak-check gate complete without ever running a leak checker.
4. **N09's green was curated.** CI passed at sign-off only by excluding the failing gtest and running a smoke subset. N09.5 fixed this properly two days later, but N09's `completed` predates the fix.
5. **Compose `moveit` service can never start `move_group`** (`docker-compose.yml:43`, `&&` after a non-exiting `roslaunch`). Untested runtime path.
6. **Historic Docker-build claims are unverifiable** — N09.5/N10 (Jul 15) builds left no image (only `n07`, Jul 14, survives). Moot for correctness (current source builds), but log claims of "passed" runs have no surviving artifact.

Cleared suspicions (for the record): Docker 29.6.1 / Compose v5.1.4 version strings in logs are genuine on this host; ci.yml contains **no** unjustified filters today; the two test suites are substantive, not stubs; the 17-vs-18 package discrepancy is the catkin prebuild pseudo-package.

---

## Ponytail audit

Project-added code (tests, CI, Docker, compose) is lean — helpers have multiple callers, YAML anchors used instead of duplication, no speculative abstraction. Findings are dead weight, ranked biggest cut first:

- `delete:` `baxter_moveit_config/config/gazebo_baxter (copy).urdf` (62 KB) + `gazebo_controllers (copy).yaml` + `baxter_sim_examples/scripts/ik_pick_and_place_demo (copy).py`. Stray "(copy)" duplicates from 2024. Replacement: nothing. [3 files]
- `stdlib:` `baxter_interface/src/baxter_dataflow/weakrefset.py` (~120 lines, vendored py2 backport, **zero importers** since `signals.py` moved to `weakref.WeakSet`). Replacement: already done — delete the file. [baxter_interface/src/baxter_dataflow/weakrefset.py]
- `delete:` `baxter_simulator/baxter_simple_sim/` — completely empty directory, not a package, unreferenced. [dir]
- `delete:` `conductor/` + `.cursor/` ROS 2/Jazzy research artifacts inside the Noetic deliverable (gitignored agent debris, 40 KB). [2 dirs]
- Borderline, user's call: `docs/*.pdf` (9.4 MB of vendor manuals in-repo — deliberate offline-docs choice per docs/README.md; fine to keep).

net: ~-190 lines, -70 KB junk, -1 vendored module possible.

**Ponytail-mode notes:** zero `ponytail:` markers exist anywhere — the project's known deliberate shortcuts (valgrind gate skipped, `init_grav()` untested, compose GUI path unverified, parse-only launch checks) live only in log prose, not at the code sites where the next reader needs them. The legacy interactive `baxter_tools/scripts/smoke_test.py` (robot-required vendor script) is easily confused with the real `test/test_smoke.py`; worth a one-line README disambiguation, not code.

---

## Minimal recommended fixes (smallest first)

1. **Commit and push everything** (one commit is fine). This single action converts N09 from FAIL to real: CI runs, the badge becomes truthful, and the uncommitted-work risk (finding 1) disappears. Everything else below can ride in the same commit.
2. Delete the 5 dead items: three "(copy)" files, `weakrefset.py`, `baxter_simple_sim/`.
3. Fix `docker-compose.yml:43`: run `move_group.launch` directly (it includes `planning_context` param loading via its own args) or background the first roslaunch — one line.
4. Record the valgrind result (this audit's run) in the N05 log, or amend the N05 gate to match the evidence that exists.
5. Reopen/annotate N04–N06 statuses in MASTER_PLAN with a one-line "verified retroactively in N07/N09.5" note so the plan stops overstating what was known at sign-off.
6. N12 remains the correct next step; its GHCR workflow will only work after fix 1.

---

## Post-audit actions (2026-07-16, user-approved)

Recommended fixes 1–5 were applied the same day:

- Deleted dead weight: 3 "(copy)" files, unreferenced `weakrefset.py`, empty `baxter_simple_sim/` (net −1,729 lines in the commit).
- Fixed `docker-compose.yml` `moveit` service: `planning_context.launch` is now backgrounded and `move_group.launch` exec'd, so move_group actually starts (previously unreachable behind a never-exiting `roslaunch &&`).
- Added README disambiguation between `test/test_smoke.py` (CI suite) and legacy `baxter_tools/scripts/smoke_test.py` (robot-required tool).
- Recorded the audit valgrind result in `logs/N05_memory_leaks.log.md` (addendum) and annotated N04/N05/N06 in MASTER_PLAN.md with retro-verification notes.
- Re-validated before commit: `docker compose config` OK; full smoke suite 6/6 against the modified tree in the image.
- Committed as `70efefe` on branch `noetic-modernization`, pushed → **first-ever GitHub Actions run (29457219793): success, no failed steps**. Fast-forwarded `master` and pushed → **run 29457566150 on master: success**.

**Effect on verdicts:** N09 FAIL → resolved (CI now demonstrably runs on push and passes; README badge truthful). Cross-cutting findings 1 (uncommitted deliverables) and 5 (compose moveit bug) → resolved. N04/N05/N06 remain PARTIAL as historical process findings, now annotated in the plan. Remaining open item at the time: N12 (GHCR images + release) — since renamed and re-scoped, see addendum below.

---

## Addendum: non-kinematics scope gap (same day, follow-up review)

N04's stated scope was always "the 8 identified bugs in `baxter_sim_kinematics`" — a curated subset of N02's full audit. N02 itself found 7 P0 and 20 P1 findings across the whole SDK, most of them outside `baxter_sim_kinematics`. Nothing in N04–N11 claimed to fix those, so this isn't a process gap like the ones above — it's just unfinished audit follow-through that the plan didn't track as an open item until now. Verified directly against current source on 2026-07-16 (second pass, same day):

**P0 (crash-severity), unaddressed:**
- `N02-P0-05` — `baxter_interface/src/baxter_interface/limb.py:348,368,385` (`set_joint_positions/velocities/torques`) still assign `dict.keys()`/`dict.values()` (Python 3 view objects) directly to ROS message array fields instead of `list(...)`.
- `N02-P0-06` — `joint_trajectory_action.py` publishes a float control rate to a `UInt16` topic; `baxter_examples/scripts/joint_velocity_wobbler.py:60,69` does the same with `self._rate = 500.0`.
- `N02-P0-07` — `baxter_simulator/baxter_sim_hardware/src/baxter_emulator.cpp:392,412` index `msg.ranges[0]` with no size check; `:429` calls `nav_light.find(msg.name)->second` with no `.end()` check; `:513-522` assumes joint-state velocity/effort arrays are populated.

**P0, already fixed (confirmed, no action needed):** `N02-P0-01` (gazebo launch XML — first bytes are now the XML declaration), `N02-P0-02/03/04` (kinematics — fixed by N04, re-confirmed above).

**P1 (high-priority), unaddressed, sampled and confirmed by direct grep/read:**
- `N02-P1-06` — `xrange` still in `joint_velocity_puppet.py:69`, `joint_velocity_wobbler.py:73`; `operator.div` still in `joint_trajectory_file_playback.py:229`. These crash immediately under Python 3.
- `N02-P1-08/09` — `gripper_action_server.py`, `head_action_server.py`, and all `setup.py`/cfg shebangs are still `#!/usr/bin/env python`, not `python3`.
- `N02-P1-10` — `camera_control.py:63` still raises undefined `ROSTopicIOException`; `tare.py`, `update_robot.py`, `enable_robot.py`, `calibrate_arm.py` still read `e.strerror`.
- `N02-P1-11` — `update_robot.py:199` still calls `re.search(pattern, robot_version)` where `robot_version` can be `None` (default from `rospy.get_param(param_name, None)`).
- `N02-P1-12` — `joint_trajectory_action.py`'s `_on_trajectory_action` still calls `_get_trajectory_parameters()` and proceeds regardless of the abort it can set.
- `N02-P1-13` — `head_action.py:81` `_command_head` still calls `self._head.set_pan(position, speed, timeout=self._timeout)`, blocking the feedback/preemption loop.
- `N02-P1-15` — all 5 controllers (`baxter_position/velocity/effort/head/gripper_controller.cpp`) still call `init(robot, joint_nh)` without checking the return value.
- `N02-P1-18` — `baxter_effort_controller.cpp:88` still declares `std::string joint_name[n_joints_]` (non-standard VLA).
- `N02-P1-20` — `baxter_simulator.rosinstall` still pins `kinetic-devel`/`development` branches.

**P1, already fixed (confirmed):** `N02-P1-07` (`xacro_jade` — zero hits anywhere), `N02-P1-19` (Dockerfile — replaced wholesale by N07, base is `ros:noetic-ros-base-focal`).

**P2/P3 (21+9 findings):** not re-verified individually in this pass — lower severity (packaging/manifest/CMake-install/URDF consistency), see `logs/N02_code_audit.log.md` for the full list with file:line references.

**Action taken:** MASTER_PLAN.md's N12 was repointed from "release" to this scope (fix the P0/P1 items above). The release step was renamed to an unnumbered **Final Step**, decoupled from the N01–N12 chain, gated on explicit user approval rather than on N12 completion alone — the user wants to review the codebase personally before any release work starts.
